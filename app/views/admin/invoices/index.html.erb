<h1>Admin Dashboard - Invoices</h1>
<div id="all_invoices">
  <h3>Invoices:</h3>
  <ul>
    <% @invoices.each do |invoice| %>
      <li>Invoice #<%= link_to "#{invoice.id}", admin_invoice_path(invoice) %></li>
    <% end %>
  </ul>