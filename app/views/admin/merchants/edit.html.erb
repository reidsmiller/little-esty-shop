<h1>Edit <%= @merchant.name %>'s Info</h1>

<%= form_with url: admin_merchant_path(@merchant), method: :patch, local: true do |f| %>
  <%= f.label :name, "Name: " %>
  <%= f.text_field :name, value: "#{@merchant.name}" %>
  <%= f.submit "Update Merchant" %>
<% end %>